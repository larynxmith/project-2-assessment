
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Wacky Widgets</title>
    <link rel="stylesheet" type="text/css" href="css/style.css">
 
</head>
<body>
    <h1>Wacky Widgets</h1>

    <% if (widgets.length === 0) { %>
        <h3> No Widgets Exist!</h3>
    <% } else { %>
        <% widgets.forEach(w => { %>
                <section>
                    <h2><%= w.description %></h2>
                    <p>Quantity: <%= w.quantity %></p>
                    <form method="POST" action="/?_method=DELETE">
                        <input type="hidden" name="description" value="<%= w.description %>">
                        <input class="btn" type="submit" value="Delete">
                    </form>
                </section>
        <% }) %>
        <% } %>

        <hr>

        <h4>Add Widget</h4>

        <form method="POST" action="/">
                <input type="text" name="description" placeholder="Widget decription...">
                <input type="integer" name="quantity" placeholder="Widget quantity...">

            <input type="submit" value="Add Widget">
        </form>



    
</body>
</html>